export const dynamic = 'force-dynamic';
export const revalidate = 0;

import { TerminalLive } from '../../components/terminal/TerminalLive';

export default function TerminalPage() {
  return <TerminalLive />;
}
